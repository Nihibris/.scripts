#!/usr/bin/env bash
set -eou pipefail

# List audio devices
audio_devices=$(pactl list short sinks | awk '{print $2}') 

for device in $audio_devices
do
    echo "$device"
done | fzf | xargs -r pactl set-default-sink


